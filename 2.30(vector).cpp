#include <bits/stdc++.h>
using namespace std;

// viết hàm có sử dụng vector 

int main()
{   
    // for( vector<int>::iterator ) cấu trúc này có con trỏ 
    // for( auto it = v.begin(); int != v.end(); i++ )
    // v.push_back();
    // v.end();
    // v.begin();
    // v.size();
    //for( int i = 0 ; i < v.size(); i++ )
    //for( int x : v);
    // muốn dùng con trỏ để truy cập phẩn tử thứ 3;  thì dùng *(v.begin() + 2)
    return 0;
}

// viết hàm 